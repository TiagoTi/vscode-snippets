{
	"Debug for Behave": {
		"body": [
			"import sys, pdb; pdb.Pdb(stdout=sys.__stdout__).set_trace()"
		],
		"description": "Add python debugger with statdio config",
		"prefix": "bpdb"
	},
	"os environment get with default": {
		"body": [
			"os.environ.get('${1:ENVIRONMENT}',${0:DEFAULT_VALUE})"
		],
		"description": "Add python environment",
		"prefix": "osed"
	}
	,
	"os environment get": {
		"body": [
			"os.environ.get('${0:ENVIRONMENT}')"
		],
		"description": "Add python environment",
		"prefix": "ose"
	}
}